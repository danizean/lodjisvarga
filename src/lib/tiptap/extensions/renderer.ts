/**
 * Renderer-specific extensions.
 * Currently empty, but reserved for future HTML-rendering optimizations,
 * transformations, or custom nodes that only apply during static generation.
 */
export function getRendererSpecificExtensions() {
  return [];
}
