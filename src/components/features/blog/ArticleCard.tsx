// import { Article } from "@/types/villa";

export function ArticleCard(/* { article }: { article: Article } */) {
  return (
    <div>
      {/* TODO: Article thumbnail, title, excerpt, publish date */}
    </div>
  );
}
