// import { Villa } from "@/types/villa";

export function VillaGallery(/* { images }: { images: string[] } */) {
  return (
    <div>
      {/* TODO: Lightbox gallery with Next.js Image + swipe support */}
    </div>
  );
}
