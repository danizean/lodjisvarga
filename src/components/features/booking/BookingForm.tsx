"use client";

// import { useBooking } from "@/hooks/useBooking";
// import { bookingSchema } from "@/lib/validations/booking.schema";

export function BookingForm(/* { villaId }: { villaId: string } */) {
  return (
    <form>
      {/* TODO: Name, email, phone, date range, guests — react-hook-form + zod */}
    </form>
  );
}
